function toggleMode() {
  const html = document.documentElement
  html.classList.toggle("light")
}

/* function toggleMode() {
  const HTML = document.documentElement
  HTML.classList.toggle("light")

} */
